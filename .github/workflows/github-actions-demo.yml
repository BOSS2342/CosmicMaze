name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Deploy via FTP
      uses: SamKirkland/FTP-Deploy-Action@4.1.0
      with:
        ftp-server: ftp://zaincwilson@gmail.com.729244@na1396.pebblehost.com:21
        ftp-username: zaincwilson@gmail.com.729244
        ftp-password: WCHS2022
        local-dir: ./plugins
        server-dir: ./
